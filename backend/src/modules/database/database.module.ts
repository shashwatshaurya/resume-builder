import { Module } from "@nestjs/common";

@Module({
  imports: [
    // TODO: Add database configuration imports
  ],
  exports: [
    // TODO: Export database providers
  ],
})
export class DatabaseModule {}
